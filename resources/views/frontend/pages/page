@extends('layouts.app')

@section('title', 'Our Services')
@section('meta_description', 'Explore the range of professional services we offer.')

@push('styles')
    <style>
        /* Page-specific styles for services */
        .service-card {
            transition: transform 0.3s ease;
        }
        .service-card:hover {
            transform: scale(1.03);
        }
    </style>
@endpush

@section('content')
    <section class="mb-10">
        <h1 class="text-4xl font-bold mb-4">Our Services</h1>
        <p class="text-lg text-gray-700">We offer a wide range of solutions tailored to your needs.</p>
    </section>

    <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        @foreach($services as $service)
            <div class="service-card bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-2xl font-semibold mb-2">{{ $service->title }}</h2>
                <p class="text-gray-600 mb-4">{{ $service->description }}</p>
                <a href="{{ route('services.show', $service->id) }}" class="text-indigo-600 hover:underline">Learn more</a>
            </div>
        @endforeach
    </section>
@endsection

@push('scripts')
    <script>
        // Optional: Add interactivity or analytics tracking
        console.log('Services page loaded');
    </script>
@endpush
